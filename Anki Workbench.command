#!/usr/bin/env bash
# Double-click this in Finder to launch the workbench (it opens a Terminal
# window and starts the server). Closing that window or pressing Ctrl+C stops it.
cd "$(dirname "$0")"
exec ./start.sh
